version: "3.9"

services:
  backend:
    build: .
    container_name: fastapi-playwright
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped
    volumes:
      - .:/app
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
